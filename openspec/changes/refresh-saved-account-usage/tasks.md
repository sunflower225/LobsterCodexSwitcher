## 1. OpenSpec And Data Flow

- [x] 1.1 完成 proposal/design/specs，明确离线账号刷新 token 与 usage 的约束
- [x] 1.2 梳理当前账号档案、usage 缓存与界面展示之间的数据流

## 2. Token Refresh Implementation

- [x] 2.1 新增基于 refresh token 的 OpenAI OAuth 刷新函数
- [x] 2.2 新增账号档案读写工具，只更新 token 字段与 `last_refresh`
- [x] 2.3 将单账号 usage 刷新改造成“必要时先 refresh token，再请求 usage”

## 3. List Refresh And UX

- [x] 3.1 改造“查看所有账号余量”逻辑，使当前账号和存档账号都能按文件来源刷新
- [x] 3.2 为账号信息补充刷新状态，区分已刷新、缓存、认证失效
- [x] 3.3 确保 5 小时 / 每周重置时间统一使用 `reset_at_*` 格式化展示
- [x] 3.4 将列表中的 5 小时 / 每周列改为“剩余百分比 + 紧凑重置时间”格式
- [x] 3.5 将当前账号合并进统一列表，并增加 PLAN 列
- [x] 3.6 在查看余量页面底部增加当前账号存档入口和按编号切换账号入口
- [x] 3.7 将刷新阶段改为进度条展示而不是逐行 bullet 输出
- [x] 3.8 将当前账号标识改为 `[当前]`
- [x] 3.9 切换成功后安排关闭并重启运行中的 Codex Desktop / CLI
- [x] 3.10 移除主菜单，默认启动直接进入查看余量页面
- [x] 3.11 将 `0=退出` 和 `Enter=刷新当前页` 收敛到查看余量页面
- [x] 3.12 切换账号成功后保持当前界面，不再主动退出工具
- [x] 3.13 重启策略改为关闭客户端/CLI，但只重启客户端
- [x] 3.14 将批量 usage 刷新改为并发实时请求，减少总等待时间
- [x] 3.15 切换成功后不再等待回车，自动刷新当前余量页面

## 4. Verification

- [x] 4.1 运行脚本检查并验证语法无误
- [x] 4.2 通过真实账号文件验证列表刷新会更新未激活账号的 usage 缓存
- [x] 4.3 更新 README 说明新的刷新行为和状态含义
